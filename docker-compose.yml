services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: bnb_ai
      POSTGRES_USER: bnb
      POSTGRES_PASSWORD: bnb
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
